name: Published source Ubuntu 26.04

on:
  schedule:
    - cron: '51 3 * * 1'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    uses: ./.github/workflows/check-source.yml
    with:
      ubuntu: '26.04'
      source: published
